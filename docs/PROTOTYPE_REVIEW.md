# 设计 v2 交付检查

2026-09-09。验证对象为 `design/prototype.html` 与规划文档。原生应用尚未开发。

## 本轮已验证

- 默认折叠，高约 294 CSS px；无小时/分钟/秒钟标签。底部箭头展开与收起，aria-expanded 与 inert 同步。
- 主窗与迷你窗内均有中 / EN 两个可见按钮，选中态正确。切换语言保持折叠状态；时区变化不改变 UTC 目标。
- 八个状态均可切换。预告/临近/重置机会/等待的数字颜色分别不同，具有相应图标和文字；失败保留演示计时并有单独失败状态。
- 大窗、迷你、拖动、隐藏、恢复、模拟刷新、深浅色和降低透明度可操作，未捕捉到 JavaScript 异常。
- 1440 × 960 画布正常；390 × 844 的英文工具栏不重叠，语言按钮仍可见。
- 键盘 Enter 可展开详情；收起详情后 Tab 不进入隐藏区域。模拟减少动态效果时动画为 none、过渡为 0s。
- 已查看主场景、中文重置机会、迷你窗和英文深色截图。深色截图在过渡完成后取得，文字保持清晰。
- 文档本地链接有效；中英文文案键一致；来源/技术选择/开发计划/验收条件均同步到免费订阅 v2。

## 数据来源核查

匿名、不带 Cookie/Authorization/API 密钥，读取 ModelYard RSS 和 Codex Reset Atom 返回 200，并可解析 XML；也核查了免费 JSON 候选。详情及缓存限制见 SOURCES.md。这是一次实网探查，不是后台监测已经实现或长期可用性证明。

免费 feed 约 5 分钟缓存，设计已取消首版的 1 分钟发现保证。RSS 内容时间、传输成功时间、上游采集时间分别处理；概率信号不进入确定倒计时。

## 尚未验证

原生 NSPanel 跨应用置顶、Liquid Glass 真机效果与旧系统回退、后台自动监测、系统通知/焦点/锁屏/登录启动、声音听感、原生时间解析器、签名安装包、GitHub 发布。相关门槛在开发计划与验收清单中。付费 X API 已移出首版，不再构成验收前置条件。

所有预览公告与查询状态均为合成数据，不是当前真实重置消息。
